# 《线性代数及应用》AI 工作入口

本文件是 AI 在本仓库中的总入口。它只保留所有任务都需要遵守的核心规则；详细要求按
任务类型分布在 `BOOK_PLAN.md` 和 `AI_GUIDE/` 中。

## 1. 指令优先级

1. 用户在当前任务中的明确要求；
2. 本文件；
3. `AI_GUIDE/` 中与当前任务相关的专项指南；
4. `BOOK_PLAN.md` 中的章节规划；
5. 相邻章节已经形成的局部写法。

若不同文件之间存在冲突，不要自行扩大修改范围，应保留用户原意并说明冲突。

## 2. 按任务读取文档

| 任务 | 必须读取 |
| --- | --- |
| 修改或新增教材正文、定义、定理、证明、小结、习题 | `AI_GUIDE/TEXTBOOK_WRITING_STYLE_GUIDE.md`、`AI_GUIDE/NOTATION_GUIDE.md` |
| 新增章节、调整章节顺序或规划后续内容 | `BOOK_PLAN.md`、`AI_GUIDE/TEXTBOOK_WRITING_STYLE_GUIDE.md` |
| 新增或修改 Asymptote 图片 | `AI_GUIDE/ASYMPTOTE_STYLE_GUIDE.md` |
| 引入尚未在前两章出现的数学记号 | `AI_GUIDE/NOTATION_GUIDE.md`，并在完成后更新该指南 |
| 编写习题答案或参考解答 | 先查看 `AI_GUIDE/README.md` 中对应指南的状态；未启用前不要擅自建立答案体系 |

专项指南的索引与成熟度见 `AI_GUIDE/README.md`。

## 3. 不可违反的核心规则

- 本书使用中文教材语体：自然、克制、准确，不使用模板化的 AI 腔。
- 新概念必须建立在已经定义的概念之上，不提前使用尚未引入的术语。
- 数学正确性优先于措辞顺滑；量词、底域、有限维条件、非空条件和环境空间必须完整。
- 编辑现有内容时坚持最小改动，不重写无关段落，不进行全文件格式化。
- 向量使用 `\bm v`，零向量使用 `\bm 0`；空间用“维数”，有限向量组用“秩”。
- 行内公式使用 `\(...\)`，行间公式使用 `\[...\]`；不用 `$$...$$`。
- 公式间距只使用 `\quad`，不使用 `\qquad`。
- LaTeX 强调只使用 `\textbf{...}`，不得混入 Markdown 的 `**...**`。
- 习题中不把“设”单独放在一行；较长对象用完整句子引出。
- 涉及图片时复用公共 Asymptote 风格，不在单图中另建冲突样式。

## 4. 修改与验证

1. 修改前阅读目标位置的前后文并检查 `git status`，保留用户已有改动。
2. 使用最小补丁完成任务，不改动任务范围外的文件。
3. 修改后运行 `git diff --check`。
4. 涉及 `.tex`、图片或构建配置时运行 `./build.sh`。
5. 检查新增标签是否唯一、引用是否解析、例题和习题数据是否经过验算。
6. 除非用户明确要求，不要自行暂存、提交或推送改动。

## 5. 维护文档体系

- 新的全书级写作约定写入 `AI_GUIDE/TEXTBOOK_WRITING_STYLE_GUIDE.md`。
- 新的统一数学符号写入 `AI_GUIDE/NOTATION_GUIDE.md`。
- 新的绘图约定写入 `AI_GUIDE/ASYMPTOTE_STYLE_GUIDE.md`。
- 章节状态、依赖关系和候选目录写入 `BOOK_PLAN.md`。
- 只有当某类任务已经实际出现且规则足够明确时，才把占位指南升级为独立文件。
- 新增专项指南后同步更新 `AI_GUIDE/README.md`，并在必要时更新本文件的路由表。
